# ========================================
# Yamoyo - 로컬 HTTPS 환경 변수 템플릿
# ========================================
# 사용법:
# 1. 이 파일을 .env.local 로 복사
# 2. 각 값을 실제 값으로 채우기
# 3. OAuth2 담당자만 GOOGLE/KAKAO 키 필요
# ========================================

# ----------------------------------------
# Database (로컬 MySQL)
# ----------------------------------------
DB_HOST=db
DB_PORT=3306
DB_NAME=yamoyo_local
DB_USERNAME=yamoyo_local
DB_PASSWORD=local_password

# ----------------------------------------
# Redis (로컬)
# ----------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379

# ----------------------------------------
# JWT 서명 비밀키
# ----------------------------------------
# 생성 방법: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
JWT_SECRET=your-jwt-secret-key-here

# ----------------------------------------
# OAuth2 - Google (OAuth2 담당자만 설정)
# ----------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://local.yamoyo.test/login/oauth2/code/google

# ----------------------------------------
# OAuth2 - Kakao (OAuth2 담당자만 설정)
# ----------------------------------------
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=https://local.yamoyo.test/login/oauth2/code/kakao

# ----------------------------------------
# OAuth2 로그인 성공 후 프론트엔드 리다이렉트 URL
# ----------------------------------------
OAUTH2_REDIRECT_URI=https://local.yamoyo.test/oauth/callback

# ----------------------------------------
# Firebase (알림 담당자만 설정)
# ----------------------------------------
FIREBASE_KEY_PATH=firebase-adminsdk.json